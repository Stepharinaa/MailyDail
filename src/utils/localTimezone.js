function convertToLocalTimeZone(data) {
  return new Date(data).toLocaleString(undefined, {
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit",
    hour12: false,
  });
}

export default convertToLocalTimeZone;
